
#include <iostream>
#include<vector>
#include <limits.h>
#include <algorithm>
using namespace std;
int f(int a, int b){
    if(b>a) return f(b, a);
    if(b==0) return a;
    return f(b, a%b);
    
    
 
}

int main()
{ 
    int x = f(40, 48);
    
    cout<<x<<"\n";
    return 0;
}
